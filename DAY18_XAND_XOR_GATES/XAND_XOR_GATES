DESIGN CODE :
module Xand_Xor(input a,b, output x,y );
  assign x= !(a & b) ;
  assign y= !(a | b);
endmodule

TESTBENCH CODE :
module tb;
  reg a,b;
  wire x,y;
  Xand_Xor u1(a,b,x,y);
  initial begin
    $dumpvars(0, tb);
    $monitor($time," | XAND: a=%b b=%b -> x=%b | OR: a=%b b=%b -> y=%b |",a,b,x,a,b,y);
            a=0;b=0; #10
    		    a=0;b=1; #10
    	    	a=1;b=0; #10
 			      a=1;b=1; #10	
    	      $finish ;
  end
endmodule
